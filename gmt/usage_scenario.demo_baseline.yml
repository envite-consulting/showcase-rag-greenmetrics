---
name: RAG Showcase - Full Baseline
author: envite Consulting GmbH
description: Full hosted-service baseline for comparing RAG configurations in the GMT dashboard. Uses a preloaded Ollama model, keeps retrieval simple, and uses a larger question workload than the lightweight default scenario.

compose-file: !include docker-compose.demo.yml

services:
  rag-app:
    environment:
      CHUNKING_STRATEGY: "simple"
      CHUNK_SIZE: 192
      CHUNK_OVERLAP: 24
      POST_BM25_RERANK: "False"
      TOP_K: 5
      OLLAMA_MODEL: "llama3:8b"
      RAG_QUESTIONS_PATH: "/src/scripts/questions.demo.json"
      RAG_QUESTION_LIMIT: 0
      MAX_TOKENS: 512

flow:
  - name: Prepare Dataset
    container: rag-app
    commands:
      - type: console
        command: python scripts/get_dataset.py --selection scripts/dataset.demo.json --force
        log-stdout: true
        read-notes-stdout: true

  - name: Warmup Indexing
    container: rag-app
    commands:
      - type: console
        command: python -m app.warmup_indexing
        log-stdout: true
        read-notes-stdout: true

  - name: Indexing
    container: rag-app
    commands:
      - type: console
        command: python -m app.indexing
        log-stdout: true
        read-notes-stdout: true

  - name: Warmup RAG
    container: rag-app
    commands:
      - type: console
        command: python -m app.warmup_rag
        log-stdout: true
        read-notes-stdout: true

  - name: RAG Queries
    container: rag-app
    commands:
      - type: console
        command: python scripts/rag_queries.py
        log-stdout: true
        read-notes-stdout: true
